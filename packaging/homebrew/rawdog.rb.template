# Author: Nicholas Corrieri

class Rawdog < Formula
  include Language::Python::Virtualenv

  desc "RAW photo managing tool that can fetch, copy, and audit your RAW libraries"
  homepage "https://github.com/nickcorrieri/rawdog"
  url "https://github.com/nickcorrieri/rawdog/archive/refs/tags/v0.1.0.tar.gz"
  sha256 "REPLACE_WITH_RELEASE_TARBALL_SHA256"
  license "Apache-2.0"

  depends_on "python@3.12"

  # Fill these resource blocks with hashes from the release dependency lock step.
  # resource "platformdirs" do
  #   url "https://files.pythonhosted.org/packages/.../platformdirs-...tar.gz"
  #   sha256 "..."
  # end
  #
  # resource "pydantic" do
  #   url "https://files.pythonhosted.org/packages/.../pydantic-...tar.gz"
  #   sha256 "..."
  # end
  #
  # resource "rich" do
  #   url "https://files.pythonhosted.org/packages/.../rich-...tar.gz"
  #   sha256 "..."
  # end
  #
  # resource "typer" do
  #   url "https://files.pythonhosted.org/packages/.../typer-...tar.gz"
  #   sha256 "..."
  # end

  def install
    virtualenv_install_with_resources
  end

  test do
    assert_match "RAW photo managing tool", shell_output("#{bin}/rawdog --help")
  end
end
